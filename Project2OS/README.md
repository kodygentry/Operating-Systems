Tested using TXST servers:

compile & run:

gcc -std=c11 -o sort sort.c
gcc -std=c11 -o pre pre.c
gcc -std=c11 lab2.c
./a.out

commands: 

ls
ls -t -al
cat file.txt 
ls -al > output.txt
ls | more | wc
./pre < input.txt | ./sort > output.txt  
exit